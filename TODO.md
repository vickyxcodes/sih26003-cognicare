# TODO

Mirrors the FEATURES sections of the spec. An item is checked only once it is implemented **and** verified (by an executed test, the static checker, or a documented manual browser check).

`[x]` done and verified &nbsp;·&nbsp; `[~]` implemented, but the last verification step needs a browser or the Firebase console (see README) &nbsp;·&nbsp; `[ ]` not started.

## Patient mode

- [x] 1. Elderly-accessible design system: 80px+ tap targets, high contrast, 2-4 choices max, no swipe/multi-touch, 6-minute session cap, always-visible home button, generous whitespace, large type
- [x] 2. Game domain 1 `memory_recall`: 6+ recall questions, large tappable picture/label options - 26 questions, 8+ per tier, played end to end in `tests/sessionEngine.test.js`; the picture set is bounds-checked in `tests/art.test.js` and eyeballed via `npm run preview:pictures`
- [x] 3. Game domain 2 `routine_matching`: 6+ object-to-routine questions, same interaction pattern - 24 cues, 8 per tier, in `src/data/routineMatching.js`; played end to end on the same `sessionEngine` in `tests/sessionEngine.test.js` (the domain-1 assertions are re-run against this bank), and the cues are checked in `tests/questions.test.js` for two options, a real answer, no answer-leaking wording and a genuine tier-3 pairing
- [~] 3b. Game domain 3 `word_recall`: show a small set of familiar English words, hide them, ask which word was just shown, 2-4 large choices, one correct - 24 rounds (8 per tier) in `src/data/wordRecall.js` on the *same* engine, screen, storage and adaptive difficulty (no new game loop, no AI/ML). Tier 1 is 2 words / 2 choices, tier 3 is 4 / 4 with every wrong word a near neighbour of a shown one. Played end to end and fully data-checked in `tests/recallGames.test.js`; the memorise-row rendering is on the README browser checklist
- [~] 3c. Game domain 4 `number_sequence`: show a short single-digit sequence, hide it, ask a plain recall/position question, 2-4 large choices, one correct - 24 rounds (8 per tier) in `src/data/numberSequence.js` on the same shared systems. Tier 1 "which did you just see?", tier 2 "which came last?", tier 3 "which came first?"; digits are spoken as words so the voice never says "forty-seven". Covered in `tests/recallGames.test.js`; the card row is on the README browser checklist
- [~] 3d. Game domain 5 `pattern_matching`: show a short row of coloured shapes, hide it, present 2-4 candidate rows, one correct, strictly tap-only (no drawing, drag, swipe or multi-touch) - 24 rounds (8 per tier) in `src/data/patternMatching.js` and `src/components/CardRow.jsx` on the same shared systems. Tier 1 unrelated wrong rows, tier 2 one-cell differences, tier 3 re-orderings of the same four shapes; each shape has its own outline *and* colour. Covered in `tests/recallGames.test.js`; shape legibility on a real screen is on the README browser checklist
- [~] 4. Adaptive difficulty: rolling streak, 2 wrong drops a tier, 3 right raises a tier, tiers 1-3, unmistakable 2-second spoken + animated transition - the streak rules, the clamping and the 2000ms animated transition are done and covered by 10 cases in `tests/sessionEngine.test.js` (and re-asserted against domain 2, which shares the same engine); the transition is now spoken through the `speak()` helper (item 7), read from the same sentence it renders; how the animation reads and how the line sounds at arm's length are on the README browser checklist
- [x] 5. Every answer written to IndexedDB immediately (domain, correct, difficultyTierEnd, timestamp) - `src/lib/store.js` over an injected driver, wired into `/play` on the tap itself; `tests/store.test.js` plays a session in each domain and checks the rows match what was played
- [~] 6. Reminders: medicine / hydration / appointment, scheduled locally, voice-announced, one-tap Done, auto-miss on timeout, logged immediately - the schedule is bundled data (`src/data/reminders.js`), the rules are pure and clock-injected (`src/lib/reminderEngine.js`) and the card lives in the patient shell (`src/components/ReminderOverlay.jsx`); `tests/reminderEngine.test.js` ticks two whole simulated days through the same engine calls the overlay makes - covering the 30-minute due window to the millisecond, the weekday filter, de-duplication against today's stored events, the Done tap, the single repeat at 45s, the automatic `missed` at 90s and the write of every event through the real store - and reads the events back to prove a reload re-announces nothing; hearing the announcement and watching the card cover a running game are on the README browser checklist
- [~] 7. All prompts, feedback, transitions and reminders spoken through one reusable `speak()` helper - the helper is built (`src/lib/speak.js`, pure and injected; `src/lib/voice.js` the only file touching the browser API, enforced by a confinement test) and wired into every screen of both game domains and into the reminder card; `tests/speak.test.js` proves newest-prompt-wins, prime-once, silence-when-unsupported and the wiring, and `tests/reminderEngine.test.js` proves the reminder speaks the same sentence it renders through the same helper; the audio itself is on the README browser checklist
- [~] 8. Full offline function after first load - precache and navigation fallback asserted by `tests/offline.test.js` and enforced by `npm run build`; the DevTools offline reload is step 1 of the README checklist
- [~] 9. Sync layer: push unsynced IndexedDB records to Firestore when online, tagged with pairingCode, queue and retry on failure - `src/lib/sync.js` holds every rule as pure logic over an injected remote and `src/lib/remote.js` is the only file that touches Firestore; the document id is derived from the local row (`sessions-<deviceId>-<rowId>`) so a retry after an uncertain result cannot create a second record, and a row is marked synced only after a server acknowledgement or a `getDocFromServer` confirmation. `tests/sync.test.js` runs the required online → offline → reconnect sequence end to end against a fake Firestore that enforces the create-only rules, proves no duplicate under an unacknowledged write, across a simulated reload and across repeated flushes, proves a failure never deletes a local record and that a retry recovers it, and drives the real start-up/reconnect/queue-nudge/stop loop on live timers; four deliberate mutations of `sync.js` were run to confirm those tests fail when the behaviour is broken. Talking to the real SDK - anonymous sign-in, the published rules accepting these documents, one document per row in the console - needs a browser and a project and is the new "Firestore sync" section of the README checklist

## Caregiver mode

- [~] 1. Pairing code entry, remembered locally after first visit - `/caregiver` takes the code once and stores it under its own `caregiverCode` setting, deliberately separate from the device's own `pairingCode`; a stored code redirects straight to the dashboard, and "Change code" clears it. No username, password, patient name or account anywhere. `tests/caregiverData.test.js` covers normalisation (lower case, spaces, a dash), the three refusal messages, persistence across a rebuilt store, and that the two settings keys never touch each other; `tests/caregiverFlow.test.js` runs the pair - reload - dashboard journey end to end and pins both redirects by reading the page source. Typing on a real keyboard is on the README checklist
- [~] 2. Per-domain Chart.js trend line - one chart per domain from `src/data/banks.js`, score on a pinned 0-100 axis with the difficulty tier as a stepped line on a 1-3 axis, last 10 sessions oldest-first. `src/lib/chartSpec.js` holds the configuration as plain data so `tests/trends.test.js` can assert the axis ranges, the tooltip wording and the reduced-motion switch without a browser; `src/components/TrendChart.jsx` is the only file that imports `chart.js` (lazily) and owns the instance, destroying it on data change and on unmount. Insufficient data never draws a misleading line: an empty series renders "Nothing to chart yet." and a single session is plotted but labelled as not a trend. A real canvas drawing and the "Canvas is already in use" cleanup check are on the README checklist
- [~] 3. Plain-language interpretation under each chart, never a bare number - both insufficient-data branches now lead with "There is not enough information to determine a trend", which is the only Step 11 wording Step 12 changed. `describeTrend()` returns a headline sentence first and the averages only as supporting detail, comparing the recent sessions against the ones before them (`Math.ceil` window, so three sessions means two against one). `tests/trends.test.js` asserts a headline never opens with a digit, that the wording matches the direction of the line in every branch, that too little data is said plainly, and runs a clinical-vocabulary blocklist (`diagnos*`, `dementia`, `impair*`, `deteriorat*`, `cognitive decline`, ...) over the headline, the detail and the difficulty sentence; `NOT_A_DIAGNOSIS` is rendered on the page, which a source test checks
- [~] 4. Decline alert card for 3+ consecutive downward sessions, explicitly non-diagnostic - `describeDecline()` / `declineAlerts()` in `src/lib/trends.js` judge each domain on the *same* `buildSeries` output the chart draws, counting the falling run backwards from the newest session (a recovered decline is not raised) and requiring a total drop of at least `DECLINE_MIN_DROP` = 5 points so a 80 → 79 → 78 slide cannot alert. `src/components/DeclineAlert.jsx` renders the exact required headline from an imported constant, with "This is not a diagnosis." and the innocent explanations inside the same card, in warn colours and `aria-live="polite"`, and renders `null` when nothing fired. `tests/decline.test.js` (28 tests) covers 0/1/2 sessions, exactly 3, more than 3, recovery, a mid-run interruption, rising, identical scores, a too-small fall at its exact boundary, both domains independently and together, interleaved rows that would look like a decline without the domain filter, and unusable scores - plus the literal wording, the disclaimer's placement inside the card, and a clinical-vocabulary blocklist. Eight mutations confirmed the suite fails when the rule is broken. How the card *reads* on a real screen is on the README browser checklist
- [~] 5. Reminder-dismissal log - `buildReminderLog()` lists the last 12 events newest first as "Medicine / Marked done" and "Water / No answer" with readable times, drops any row whose type or status is not one the app itself writes, and adds a counting summary. Covered in `tests/caregiverData.test.js` and asserted end to end against a mixed history in `tests/caregiverFlow.test.js`; seeing it in a browser is on the README checklist
- [~] 6. "Last synced" timestamp so stale data cannot mislead - `describeFreshness()` returns a value *and* a sentence saying which of the three possible times it is (the dashboard's read, this device's last upload, or nothing yet), plus a separate "latest activity" line and a staleness warning. It never prints the current clock as a sync time: with nothing read the value is "Unavailable" or "Not yet". "Could not read" and "nothing recorded" produce different warnings, so a blank chart can never look like a patient who stopped playing. Covered in `tests/caregiverData.test.js`, and a mutation that falls back to the current clock fails three tests

## Platform / delivery

- [x] Vite + React + Tailwind + React Router scaffold
- [x] PWA plugin configured (manifest + Workbox precache) - asserted in `tests/pwa-config.test.js`
- [x] App icons generated and installable - `npm run gen:icons`, sizes verified from the PNG headers in `tests/icons.test.js`
- [~] Service worker verified offline - build fails without a full precache; the browser reload check is in the README
- [~] Firestore schema + security rules file - `firestore.rules` written and now exercised in tests: the sync layer only ever attempts an authenticated create carrying a string `pairingCode`, never an update or a delete, and a test fails if the rules gain an extra `allow create`, lose `request.auth != null`, or ever say `if true`. Caregiver reads needed **no rules change** - step 10 already published `allow read: if request.auth != null` on both collections - and a step-11 test now asserts every `allow read|get|list` still carries that condition, so a future attempt to loosen one fails the suite. The `patients` collection stays unused: pairing writes nothing remote, because a person-shaped record on the server is exactly what the privacy constraint forbids. Publishing the rules and running the Rules Playground check is still a console action
- [~] Firestore sync from IndexedDB, queued offline and retried on reconnect - see patient item 9; verified against a fake Firestore, not yet against a real project
- [x] Demo history seeding, easy to re-run and reset - done. `src/lib/demoData.js` fabricates 12 days of realistic play history (17 sessions: 9 memory_recall, 8 routine_matching, plus 136 answer rows and 12 reminder events) into local IndexedDB with `demo: 1`, so the dashboard, both charts and the Step 12 decline alert are populated on first demo run. Memory_recall falls 75→81→78→84→76→71→64→58→49 (a 4-session falling run totalling 22 points) and routine_matching stays in the low-to-mid 80s. The trigger is `window.cognicare.seedDemo()` or `?demo=seed`; reset is `window.cognicare.resetDemo()` or `?demo=reset`; `window.cognicare.demoStatus()` shows demo vs real row counts; the URL parameter is stripped after handling. The reset is surgical: only `demo: 1` rows are deleted; the pairing code, device id, `lastSyncAt`, real patient history and all settings are untouched. 35 new tests in `tests/demoData.test.js` cover schema, domains, day span, the decline alert, reset safety, dashboard consumption, determinism, idempotency, and the mechanism itself. Triggering and confirming the seeded dashboard in a real browser is on the README checklist
- [x] GitHub Pages deploy path (`predeploy` + `deploy`) with SPA 404 fallback and `.nojekyll`
- [x] CURRENT_STATE.md / DECISIONS.md / TODO.md / README.md kept current

